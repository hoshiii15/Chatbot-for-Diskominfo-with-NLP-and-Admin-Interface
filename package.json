{
  "name": "faq-chatbot-system",
  "version": "1.0.0",
  "private": true,
  "description": "Complete admin dashboard system for FAQ Chatbot with Express.js backend and Next.js frontend",
  "workspaces": [
    "shared",
    "admin-backend",
    "admin-frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev:*\"",
    "dev:python": "cd python-bot && ./venv/bin/python app.py",
    "dev:backend": "npm run dev --workspace=admin-backend",
    "dev:frontend": "npm run dev --workspace=admin-frontend",
    "build": "npm run build --workspace=shared && npm run build --workspace=admin-backend && npm run build --workspace=admin-frontend",
    "install:all": "npm install && cd python-bot && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt",
    "start": "npm run prod:start",
    "prod:start": "pm2 start ecosystem.config.js",
    "prod:stop": "pm2 delete ecosystem.config.js",
    "prod:restart": "pm2 restart ecosystem.config.js",
    "prod:logs": "pm2 logs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "author": "Diskominfo",
  "license": "MIT"
}

